[phases.install]
cmds = ["corepack enable", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm typecheck", "pnpm check", "pnpm lint", "pnpm check:generated", "pnpm build"]

[start]
cmd = "pnpm start"
