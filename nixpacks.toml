[phases.setup]
cmds = []

[phases.install]
cmds = ["npm install --global pnpm@11.3.0", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm typecheck", "pnpm check", "pnpm lint", "pnpm check:generated", "pnpm build"]

[start]
cmd = "pnpm start"
